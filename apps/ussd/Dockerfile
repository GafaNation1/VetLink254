# apps/ussd/Dockerfile — Container specification for VetLink254 USSD thin adapter service
FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8001

# Flask WSGI app served by gunicorn; uvicorn not used here (Flask is the USSD adapter stack per architecture Section 8).
CMD ["gunicorn", "--bind", "0.0.0.0:8001", "--workers", "2", "app.main:app"]