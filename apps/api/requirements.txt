# apps/api/requirements.txt — Pinned Python dependencies for VetLink254 Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
# FastAPI Form/File parsing (multipart KYC document upload, PART 4).
python-multipart==0.0.9
sqlalchemy==2.0.31
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
redis==5.0.7
# Africa's Talking SMS via the official SDK. PINNED to 2.0.3: the SDK auto-routes to the sandbox
# when the username is "sandbox" and hardcodes its base URLs (AT_SMS_BASE_URL is dead config).
africastalking==2.0.3

# Minimal real auth (PART 3): bcrypt password hashing + HS256 JWTs.
bcrypt==4.2.1
PyJWT==2.9.0

# KYC file storage (PART 4): boto3 for Cloudflare R2 (S3-compatible). R2 fallback = local disk.
boto3==1.34.151

# Test dependencies (run `pytest` from apps/api; see apps/api/README.md for the exact command)
pytest==8.2.2
pytest-cov==5.0.0
httpx==0.27.0
